---
- hosts: all
  name: Install web servers soft
  gather_facts: false
  become: true
  become_method: sudo
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install soft
      apt:
        pkg:
          - haproxy
          - nginx
