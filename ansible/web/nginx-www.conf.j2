server {
  listen 127.0.0.1:10001;

  root {{ document_root }}; # TODO via var
  index index.html;

  server_name {{ server_name }};

  location / {
   #default_type "text/html";
   try_files $uri.html $uri $uri/ =404;
  }
}